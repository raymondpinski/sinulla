// Set the even-odd rule to `true`
const useEvenOddRule = true;

// Use the even-odd rule when filling a shape
if (useEvenOddRule) {
    // ... code to fill the shape using the even-odd rule ...
} else {
    // ... code to fill the shape using a different rule ...
}
