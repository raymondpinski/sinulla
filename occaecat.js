// Get all elements with the same ID (though IDs should be unique)
const allElements = document.querySelectorAll('#my-element');

// Now you can use NodeList methods on allElements
console.log(allElements.length); // 1
